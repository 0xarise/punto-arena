{
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "gunicorn --worker-class eventlet -w 1 --bind 0.0.0.0:$PORT app_wagering:app",
    "healthcheckPath": "/api/leaderboard",
    "restartPolicyType": "ON_FAILURE"
  }
}
