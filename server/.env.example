# Oracle private key for signing game results
# Generate with: python -c "from eth_account import Account; a = Account.create(); print(f'ORACLE_PRIVATE_KEY={a.key.hex()}\nORACLE_ADDRESS={a.address}')"
ORACLE_PRIVATE_KEY=0x...

# Server config (optional)
HOST=0.0.0.0
PORT=8000
